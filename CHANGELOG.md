# 更新日志

## 2.1.0

### 新增

- **半高功能项**：功能项可勾选「半高」，只占半行，相邻两个半高项上下叠进同一格，
  实现「两边整高、中间塞两个半高」的排布。落单半高项下方会出现半格大小的「+」，点击直接补一个半高项
- **行高可调**：全局设置和单个分组都能设置网格行高（默认 90），半高项占其一半
- **真实预览**：画布右上角新增「👁 真实预览」，用出图时同一套 Pillow 渲染当前（未保存的）菜单

### 变更

- 功能项文字放不下格子时改为自动取舍：先削减描述行数并补省略号，不够再隐藏描述，名称至少保留一行。
  以前内容会画到卡片外面，现在同样的菜单里溢出的描述会被截断或隐藏
- 编辑器网格项去掉了 `min-height: 80px` 和上下内边距，格子高度完全由网格行高决定，与出图一致

## 2.0.0

编辑面板从独立端口迁移进 AstrBot WebUI，并修掉一批触发和渲染问题。

> ⚠️ 本版本要求 **AstrBot ≥ v4.26.0**（插件页面机制从这个版本开始提供）。

### 架构

- 编辑面板改为 AstrBot 插件页面（`pages/菜单编辑器/`），在 WebUI → 插件管理里直接打开
- 接口通过 `context.register_web_api()` 注册，鉴权复用 WebUI 登录态
- 删除 `web_server.py`：不再 spawn 子进程、不再监听独立端口、不再自建密钥登录页
- 前端请求改走 AstrBot 注入的 `window.AstrBotPluginPage`，素材取回后转 blob URL 缓存，
  素材面板用服务端生成的缩略图，避免整张原图过桥
- 配置项 `web_host` / `web_port` / `web_token` 随之移除，新增
  `enable_natural_language`、`file_fallback_mb`

### 修复

- **指令触发不稳定**：`/菜单` 及别名改用 `CommandFilter` 并带正优先级；
  自定义触发词与自然语言兜底降为负优先级，不再与其它插件互相抢占事件
- **每条群消息都读盘**：触发词改为内存索引，未命中时不读盘、不解析 JSON
- **后台"启动成功"却打不开**：成因是端口尚未绑定就上报成功（随独立端口模式一并移除）
- **插件卸载钩子从未执行**：`on_unload` 改为基类实际使用的 `terminate`
- **初始化竞态**：改用 AstrBot 会 await 的 `initialize()`，
  并移除原先超时会把初始化任务 cancel 掉的 `wait_for` 逻辑
- **长英文功能名压住描述**：名称也参与折行，高度按实际行数计算
- **折行把英文单词切碎**：改为中文逐字断行、拉丁单词整体换行，超长单词才硬断
- **出图中文变方框**：字体缺失时先回退到插件自带的中文字体，而非直接用默认点阵字体
- **没装 imageio 就完全不可用**：`imageio` / `numpy` 改为动态背景渲染时才导入
- **老用户数据未迁移**：迁移判断条件写错导致是死代码，现改为按 `menu.json` 是否存在判断
- **连发菜单重复渲染**：每个菜单加渲染锁
- **画布在 iframe 里不显示**：容器宽度未就绪时会算出 `scale(0)`，已加保护并跟随容器尺寸重绘
- 配置保存改为先写临时文件再替换，避免写一半损坏 `menu.json`

### 安全

- 移除未登录即可访问的素材、字体、成品图下载接口
- 移除「未配置管理员时任何人都能开后台」的判断（后台已不存在，权限统一由 WebUI 管理）

### 清理

- 删除死文件：`preview.py`（引用了不存在的 `render_one_menu`）、
  `renderer/base.py`（无人引用）、`renderer/themes/miao.py`（空文件）
- 从版本库移除误提交的用户数据 `data/menu.json`
