{
  "enable_natural_language": {
    "type": "bool",
    "title": "启用自然语言触发",
    "description": "开启后，群里问「你能干什么」「怎么用」这类话也会自动发菜单。关掉则只响应 /菜单 指令和你自己配的触发词。",
    "default": true
  },
  "file_fallback_mb": {
    "type": "int",
    "title": "转文件发送阈值 (MB)",
    "description": "菜单图片超过这个体积时改用文件形式发送，避免被平台压缩或发送超时。",
    "default": 15
  }
}
